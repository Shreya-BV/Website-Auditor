[phases.setup]
nixPkgs = ["python311"]

[phases.build]
cmds = [
    "playwright install chromium",
    "playwright install-deps"
]

[phases.start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
